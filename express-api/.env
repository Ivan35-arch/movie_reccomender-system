PORT=3000

# Flask ML — already running standalone
FLASK_ML_URL=http://localhost:5000

# PostgreSQL — update these if you have a local Postgres running
DB_HOST=localhost
DB_PORT=5432
DB_NAME=movie_db
DB_USER=movie_login
DB_PASSWORD=seive_password
DB_SSLMODE=disable

# JWT
JWT_SECRET=seive-dev-secret-change-in-production

# CORS — allow all origins in development
ALLOWED_ORIGINS=*
